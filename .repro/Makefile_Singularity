### Singularity Command ###

SCMD = exec

ifeq ($(SINGULARITY),TRUE)
	SRUN := singularity $(SCMD) $(SFLAGS) $(PROJECT).sif
	current_dir=/home/rstudio
endif

### Singularity Image ###

singularity: $(PROJECT).sif

$(PROJECT).sif: docker
	singularity build $@ docker-daemon://$(PROJECT):latest
